{
  "name": "mobile-sync",
  "version": "1.0.2",
  "description": "OpenCode plugin for mobile sync via Tailscale Funnel",
  "type": "module",
  "scripts": {
    "build": "bun build mobile-sync.js --target=node --no-bundle --outdir .",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
